<div class="workouts">
	<h1>Are you ready to sweat?!</h1>
	<p>Select your workout playlist.</p>
	<% @workouts.each do |workout| %>
	<h2><%= workout.name %></h2>
		<a href="/workouts/<%= workout.id %>/start">Start</a>
		<a href="/workouts/<%= workout.id %>">Edit</a>
	<% end %>
</div>